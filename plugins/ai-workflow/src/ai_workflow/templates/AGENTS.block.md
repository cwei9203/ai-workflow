## AI Development Workflow

Before every task, read `.ai-workflow/workflow/entry.md` and follow the workflow
it selects. Project-specific commands and constraints are defined only in
`.ai-workflow/project.json`. Use the repository-local `./aiw` (`aiw.cmd` on
Windows) for workflow checks and task state.
